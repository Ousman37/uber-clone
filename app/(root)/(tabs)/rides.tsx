import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = (): React.ReactElement => {
  return (
    <SafeAreaView className="">
      <Text>Rides</Text>
    </SafeAreaView>
  );
};

export default Rides;
